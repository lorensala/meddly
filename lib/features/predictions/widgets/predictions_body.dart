import 'package:flutter/material.dart';

class PredictionsBody extends StatelessWidget {
  const PredictionsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(),
        Container(),
        Container(),
        const Text('Ultimas consultas'),
        Container(),
      ],
    );
  }
}
